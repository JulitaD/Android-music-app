<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lt.vcs.musicapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicApp">
        <activity
            android:name=".main.SearchResultsActivity"
            android:exported="false" />
        <activity
            android:name=".main.album.view.AlbumDetailsActivity"
            android:exported="false"
            android:label="Album details" />
        <activity
            android:name=".main.artist.view.ArtistDetailsActivity"
            android:exported="false"
            android:label="Artist details" />
        <activity
            android:name=".main.track.view.TrackSearchActivity"
            android:exported="false"
            android:label="Search results" />
        <activity
            android:name=".main.artist.view.ArtistSearchActivity"
            android:exported="false"
            android:label="Search results" />
        <activity
            android:name=".main.album.view.AlbumSearchActivity"
            android:exported="false"
            android:label="Search results" />
        <activity
            android:name=".main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>